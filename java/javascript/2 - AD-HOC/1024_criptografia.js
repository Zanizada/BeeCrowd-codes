var input = require('fs').readFileSync('/dev/stdin', 'utf8');
var lines = input.split('\n');

function passada_1(texto) {
    let caracteres = [];
    let caractere;
    let 
    for (caractere in texto) {
        if (caractere != isNaN) {
            
        }
    }
}